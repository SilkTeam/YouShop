@{
    if (Session["User"] != null)
    {
        Layout = "~/Views/Shared/_User.cshtml";
    }
    else
    {
        Layout = "~/Views/Shared/_Layout.cshtml";
    }
    ViewBag.Title = "Index";
    var list = ViewBag.list as List<YouShop.Model.Shop>;
}
<div class="row">
    @foreach (var item in list)
    {
        <div class="col-md-4">
            <div class="card mb-4 shadow-sm">
                <div class="bd-placeholder-img card-img-top" width="350" height="225" focusable="false">
                    @if (@item.Img != null)
                    {
                        <img style="width:100%;height:225px;" src="@item.Img" />
                    }
                    else
                    {
                        <img style="width:100%;height:225px;" src="~/Upload/Default.jpg" />
                    }
                </div>
                <div class="card-body">
                    <p class="card-title">@item.Name</p>
                    <p class="card-text">@item.Description</p>
                    <div class="d-flex justify-content-between align-items-center">
                        @if (Session["Identity"] != null)
                        {

                            <div class="btn-group">
                                <a type="button" href="/MyCar/Add?ID=@(item.ID)" class="btn btn-sm btn-outline-secondary">添加到购物车</a>
                            </div>
                        }
                        <small class="text-muted">@item.EntryTime 上架</small>
                    </div>
                </div>
            </div>
        </div>
    }
</div>